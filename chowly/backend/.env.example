# Copy this file to .env for local development.
# On Render, you'll set DATABASE_URL as an environment variable instead (see README).

# Render Postgres gives you this connection string when you create the database.
DATABASE_URL="postgresql://user:password@localhost:5432/chowly?schema=public"

PORT=4000
